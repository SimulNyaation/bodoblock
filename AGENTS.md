# 작업 가이드

## 제품 방향
- BlockStep은 만보기와 가벼운 게임을 결합한 모바일 중심 생산성 게임이다.
- Android Play Store와 iOS App Store 출시를 우선으로 고려한다. 웹과 앱인토스는 추후 확장 후보이다.
- 생산성 도구보다 게임에 가까운 경험을 지향한다.
- Townscaper, Stack처럼 조작과 반응 자체가 즐거워야 한다. 레퍼런스의 자산이나 디자인을 그대로 복제하지 않는다.
- 큰 목표, 경쟁, 연속 출석 압박을 기본 전제로 삼지 않는다. 걷지 않은 날에도 부담 없는 경험을 설계한다.

## 구조와 개발 원칙
- `apps/play/src/`: V2 화면, 입력, Three.js 표현, 효과음, 영수증 출력.
- `packages/play-core/`: UI나 운영체제에 의존하지 않는 V2 게임 규칙과 상태.
- V1의 `apps/mobile/`, `packages/game-core/`, `packages/activity-core/`는 로컬 보관 전용이다. V2에서 참조하지 않는다.
- `assets/`: 이미지, 사운드, 폰트 등의 원본 자산.
- `docs/`: 기획과 기술 결정 기록.
- 프로토타입은 Vite + TypeScript + Three.js를 사용한다. 현재 규칙은 `docs/minimal-v2.md`, 배포 구조는 `docs/deployment.md`에 기록한다.
- 초기에는 작은 플레이 루프부터 만든다. 요청 없이 서버, 로그인, 결제, 광고를 추가하지 않는다.
- 센서·건강 데이터 API 접근은 모바일 플랫폼 계층에 둔다. 게임 로직에서 직접 호출하지 않는다.
- 권한 거부, 걸음 데이터 미지원, 동기화 지연을 고려하고 개발 시 모의 데이터를 사용할 수 있게 한다.
- 걸음 수와 건강 관련 데이터는 최소한으로 다루고, 외부 전송은 별도 요구가 있을 때 설계한다.
- 세로 화면과 한 손 조작을 초기 가정으로 삼고, 작은 화면의 가독성과 배터리 사용량을 고려한다.
- 게임 규칙과 걸음 집계가 구현되면 의미 있는 단위 테스트를 추가한다. 센서 동작은 실제 기기에서도 확인한다.
- 기존 사용자 변경을 보존하고, 작업 결과와 검증 여부는 한국어로 간결하게 설명한다.

## 현재 상태
- 세션 최초 타일 터치(또는 테스트용 게임 조작 키) 이후에만 자동 낙하를 시작한다. 다음 타일 생성 시 시작 상태를 유지하고 새 세션에서는 초기화한다.
- 빠른 안착은 아래 방향 순이동 48 CSS px 이상·세로 우세·기존 75% 높이 조건을 함께 요구한다. 일반 자동 낙하는 스와이프 없이 유지한다. Export의 글씨는 날짜와 전체 조각 수(흰 타일 포함)만 상단 중앙에 표시하며 닫기/저장 버튼은 유지한다.
- 기본 진입점은 미니멀 V2 (`apps/play/src/`, `packages/play-core/`)다. 화면 설명·거리·클리커·자동 걷기·진행 저장을 추가하지 않는다.
- V2는 자동 낙하(초당 1.4칸), 직접 아래·좌우로 끌기(위로 이동 금지), 탭 회전, 착지 실루엣, 한 칸 빈자리 자동 채움을 중심으로 한다. 잡고 있는 동안에는 자동 낙하를 멈춘다. 손을 놓으면 원위치 복귀 없이 낙하를 이어가며, 바닥 접촉 0.45초 후 고정한다. 바닥 기준 75% 선 아래에서 수동으로 내려놓으면 빠르게 안착한다. 일시정지 메뉴는 Resume/Export만 유지한다.
- 세션은 사용자가 놓는 블록 172,800개(0.5초당 1개 × 24시간)까지다. 흰 자동 타일은 제외한다. 실제 24시간 타이머나 초당 2개 속도 제한은 아니다. 한도 후 추가 입력/자동 배치를 막고 Export는 유지한다.
- Export는 가로 688px 고정 PNG 한 장을 256행씩 스트리밍 생성한다. 3,200만 픽셀 또는 세로 65,536px 초과 시 SVG 파일 한 장으로 전환한다. 축소나 파일 분할은 하지 않는다. SVG는 공통 윤곽 재사용, 미리보기는 가상 스크롤로 DOM 수를 제한한다.
- PNG 네이티브 저장 메뉴 완료 후 초기화하며 취소·실패 시 유지한다. 메뉴 미지원 환경은 길게 누르기/저장 완료 확인을 사용한다. SVG는 파일 다운로드 요청 후 초기화하며 사진 앱 저장이 아님을 안내한다. 생성 취소/실패 시 기록을 유지한다.
- 기존 V1 소스·진입점·테스트는 원래 로컬 경로에 보존하지만 `.gitignore`로 업로드에서 제외한다. 기본 빌드/타입 검사/테스트는 V2만 실행한다. 별도 요청 없이 레거시 파일을 삭제하거나 기록을 변경하지 않는다. V2 소리 합성은 `apps/play/src/audio.ts`에 독립적으로 둔다.
- `npm run dev`: 웹 프로토타입 실행. `npm run build`: 타입 검사 및 빌드. `npm run test`: 핵심 규칙 검증.
- 네이티브 만보기 및 앱 배포 환경은 아직 구현하지 않았다. 웹 움직임 감지는 실험 기능으로 명확히 구분한다.
